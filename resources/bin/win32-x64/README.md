Place the compiled `blark.exe` CLI here when packaging the extension.

At runtime the extension first checks `twincatBlark.exePath`. If that setting
is empty, it expects this folder to contain `blark.exe`.
